# P0 — Skeleton and money type

**Half a day. Already largely scaffolded — verify rather than rebuild.**

## Goal
Make the project's defining bug structurally impossible before writing anything else.

## Tasks
- [x] Repo structure, `pyproject.toml`, `Makefile`, `.gitignore`
- [x] `src/milaan/normalize/money.py` — the `Paise` type
- [x] `src/milaan/normalize/fees.py` — gross → fee → GST → net
- [x] `tests/test_no_floats.py`, `tests/test_no_label_leak.py`, `tests/test_fee_math.py`
- [ ] `make setup && make test` green on your machine
- [ ] Confirm the float guard fires: add `x = 0.02` to a money-path module, run `make test`, watch it fail, remove it
- [ ] Pin your model version in `config/`
- [ ] First real `DECISIONS.md` entry

## Exit gate
`make test` green, and you have personally seen `test_no_floats` fail on a planted float.

## Do not
Start the generator before the money type is settled. Every downstream module depends on its semantics.
