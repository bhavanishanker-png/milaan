# P7 — Exception queue and full ablation

**Day 12.**

## Build
- `ui/app.py` — Streamlit exception queue: reason code, cluster detail, agent rationale, evidence trail, approve/reject. **One page.** Time spent on CSS is stolen from P8.
- `eval/ablation.py` — the four-row table
- Per-class recovery table across all 15 D-codes
- Throughput and latency: records/minute, p50, p95, LLM cost per 100

## The money chart
| Configuration | Auto-resolve | False match | Cost/100 |
|---|---|---|---|
| T1 only | | | ₹0 |
| T1 + T2 | | | ₹0 |
| T1 + T2 + T3 | | | |
| + RAG | | | |

If the agent adds 6 points on 4% of records for ₹2 per 100, **say exactly that**. A quantified marginal contribution beats a vague claim of AI-poweredness, and directly answers the "sensible AI usage" criterion.

## Per-class table
For each D-code: injected / resolved / escalated / missed. This is your honest exception list and doubles as a roadmap slide: "D09 and D15 are where we're weakest, here's why."

## Exit gate
Both tables complete with real numbers; UI renders one real escalated exception end to end (this is your video demo); cost measured, not estimated.

## If behind
Printed exception report instead of the UI. Never cut the ablation table.
